{% if not chunk %}
  {% set chunk = {
  classname: 'default-classname'
  } %}
{% endif %}

<div class="chunk {{ chunk.type|default('') ~ ' ' ~ chunk.classname|default('') }}">
  <div class="chunk-inner">

  </div>
</div>