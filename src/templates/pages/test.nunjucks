<html>
  <head>
    <title>Test</title>

    <style>
      .js-popup { visibility: hidden; }
      .js-popup.is-expanded { visibility: visible; }

      .js-popup {
        position: fixed;
        top: 0;
        left: 0;
        width: 100%;
        height: 100%;
      }

      .js-popup-backdrop {
        position: absolute;
        z-index: 0;
        top: 0;
        left: 0;
        width: 100%;
        height: 100%;

        background: rgba(0,0,0,0.15);
      }

      .js-popup-inner {
        position: relative;
        z-index: 1;

        width: 80%;
        max-width: 680px;
        margin: 40px auto;
        padding: 20px 20px 40px;

        background: #fff;
      }

    </style>
  </head>

  <body>

    <button class="js-ctrl" data-target="#my-popup:popup" data-action="expand">Expand Popup</button>

    <button class="js-ctrl" data-target="#my-popup:popup" data-action="collapse">Collapse Popup</button>

    <div id="my-popup" class="js-popup">
      <div class="js-popup-backdrop"></div>
      <div class="js-popup-inner">
        <h2>Popup Content</h2>
        <p>Lorem ipsum dolor sit amet, consectetur adipisicing elit. Quam nobis saepe natus quibusdam dolore. Quasi est aperiam dignissimos molestiae earum vitae accusamus, nostrum exercitationem veniam consequatur voluptates! Amet, earum, esse?</p>
        <a class="js-popup-close" href="#">Close popup</a>
      </div>
    </div>


    <!-- =jQuery -->
    {% set jQueryCdnFile = 'jquery-1.11.3.js' if app.environment == 'dev' else 'jquery-1.11.3.min.js' %}
    <script src="https://code.jquery.com/{{ jQueryCdnFile }}"></script>
    <script>window.jQuery || document.write('<script src="./js/vendor/jquery.js"><\/script>')</script>

    {% if app.watch %}
      <!-- =Scripts-watch -->
      <script src="{{ app.servers.webpackDev.url }}"></script>
    {% endif %}

    <script src="{{ 'js/main.js'|webpackurl }}"></script>
    <script src="{{ 'js/test.js'|webpackurl  }}"></script>
  </body>
</html>